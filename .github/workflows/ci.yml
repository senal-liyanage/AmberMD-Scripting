name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  shell-syntax:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Verify shell script syntax
        run: |
          bash -n generate_amber_inputs.sh
          bash -n run_md_simulations.sh
